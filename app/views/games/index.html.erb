<h1>Juegos</h1>

<%= link_to 'nuevo juego', new_game_path %>

<table>
    <thead>
        <th>Nombre</th>
        <th>Año</th>
        <th>Categoria</th>
    </thead>

    <tbody>
        <% @games.each do |game| %>
            <tr>
                <td>
                    <%= game.name %>
                </td>
                <td>
                    <%= game.year %>
                </td>
                <td>
                    <%= game.category.name %>
                </td>
                <td>
                    <%= link_to 'ver', game_path(game.id)%>
                </td>
                <td>
                    <%= link_to 'Borrar', game, method: 'delete', data: {confirm: 'seguro?'}%>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>